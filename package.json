{
  "name": "ktag-manager",
  "private": true,
  "version": "5.1.1",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev:backend & npm run dev:web",
    "dev:web": "npm run dev --workspace=@ktag/web",
    "dev:backend": "npm run dev --workspace=@ktag/backend",
    "dev:local:app": "LOCAL_FIREBASE_EMULATORS=true FIREBASE_PROJECT_ID=demo-ktag-local FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099 VITE_USE_FIREBASE_EMULATORS=true VITE_FIREBASE_PROJECT_ID=demo-ktag-local npm run dev",
    "dev:local": "firebase emulators:exec --project demo-ktag-local --only auth,firestore 'node scripts/seed-emulators.mjs && npm run dev:local:app'",
    "test:rules": "firebase emulators:exec --project demo-ktag-rules --only firestore 'node scripts/identity-rules.test.mjs'",
    "build": "npm run build --workspace=@ktag/web",
    "typecheck": "npm run typecheck --workspace=@ktag/web && npm run typecheck --workspace=@ktag/backend",
    "lint": "npm run lint --workspace=@ktag/web",
    "lint:fix": "npm run lint:fix --workspace=@ktag/web",
    "docker:build": "docker build -f packages/backend/Dockerfile -t ktag-backend:local packages/backend"
  },
  "devDependencies": {
    "graphemer": "^1.4.0",
    "typescript": "^5.2.2"
  },
  "overrides": {
    "undici": "^6.25.0",
    "uuid": "^11.1.1",
    "http-proxy-agent": "^7.0.2",
    "@tootallnate/once": "^3.0.1"
  }
}
