; NPM Configuration for K-Tag Manager
; Ensures consistent dependency resolution across CI/CD environments

# Prevent automatic peer dependency installation errors
legacy-peer-deps=false

# Ignore unknown config warnings
ignore-scripts=false
